{
  "name": "hoge",
  "version": "0.1.0",
  "scripts": {
    "dev": "yarn && yarn clean && shadow-cljs run hoge.dev/watch",
    "build": "yarn clean && shadow-cljs release app",
    "lint": "shadow-cljs run clj-kondo.main --lint src",
    "clean": "rm -rf public/js target"
  },
  "devDependencies": {
    "shadow-cljs": "2.28.20"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
